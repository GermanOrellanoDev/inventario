export class Producto {
  idProducto!: number; //con el signo de exclamación al final afirmamos que este atributo se inicializará más adelante
  descripcion!: string;
  precio!: number;
  existencia!: number;
}
